/**
 * Used by: `tests/rules/unicorn-filename-case.test.ts`
 *
 * Expected:
 * - error: `unicorn/filename-case` (filename is NOT kebab-case)
 *
 * Note:
 * - The filename itself is the test input.
 */

export const x = 1;
